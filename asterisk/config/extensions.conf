[internal]
; Ramais legados fixos
exten => 1001,1,Dial(PJSIP/1001,30)
 same => n,Hangup()

exten => 1002,1,Dial(PJSIP/1002,30)
 same => n,Hangup()

; Ramais criados pela API (2000-2999)
exten => _2XXX,1,Dial(PJSIP/${EXTEN},30)
 same => n,Hangup()

; Ramal de echo para testes
exten => 3000,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

